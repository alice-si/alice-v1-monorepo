{
  "name": "alice-monorepo",
  "private": true,
  "version": "0.1.0",
  "description": "A monorepo for Alice (https://alice.si)",
  "main": "index.js",
  "scripts": {
    "test": "source packages/donations-app/.envrc && source packages/jobs/.envrc && yarn lerna run test --stream --loglevel debug"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "mongoose": "5.3.12",
    "openzeppelin-solidity": "^1.11.0"
  },
  "devDependencies": {
    "eslint": "^5.16.0",
    "lerna": "^3.13.4"
  },
  "workspaces": ["packages/*"]
}
